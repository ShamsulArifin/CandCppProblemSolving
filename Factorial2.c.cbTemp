#include<stdio.h>
#include<conio.h>

void main()

{
 float f=1,x;

 printf("Enter Number: ");
 scanf("%f",&x);

 if(x==0)
 {
  f=f;
 }
 else
 {
    do
    {
     f=f*x;
     x=x-1;
    }while (x>1);
 }
 printf("\n\tFactorial value is: %f", f);

 getch();
}
